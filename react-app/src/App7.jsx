/**
 * React Query Intro
 * - Why use React-Query
 * 1) Library focused on making data-fetching simple and efficient
 *   * Easy to understand for other developers
 *   * Build in tools to save bandwidth
 * 2) Caching => Great for the UX ( faster & responsive )
 * 3) Customizable
 *   retries, paginated queries, prefetching, optimistic updates etc.
 * 
 * - Architecture
 * - install
 *   `npm install react-query`
 */

export default function App() {
  return (
    <>
      <p>React Query Intro</p>
    </>
  );
}